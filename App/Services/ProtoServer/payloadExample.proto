syntax="proto3";

option csharp_namespace = "ProtoServer";

service payloadExampleSerivce{
	rpc GetPayloadMessage(PayloadMessage) returns (PayloadMessage);
}

message PayloadMessage{
	string message = 1;
}